import { NavLink } from "react-router-dom";
const SideNav = function () {
    return (
        <div>
            SideNav: to be styled later
            <nav>
                <NavLink to="/ratting">
                    Ratting
                </NavLink>
            </nav>
        </div>
    )
}

export default SideNav;